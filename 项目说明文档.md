# Battleworld — 分布式多人实时战斗游戏

## 项目概述

**Battleworld**（烬原战境）是一款基于 **Go + gRPC + Redis + PostgreSQL** 构建的分布式多人实时战斗游戏。玩家通过终端客户端接入，在多张地图中移动、战斗、拾取宝物、挑战世界首领；地图状态由分布式 Node 集群承载，并通过 Gateway 实时同步。

当前架构已将 **Coordinator 控制面**、**Gateway 接入/数据面** 与 **Node 地图运行时** 拆分为独立进程。Gateway 高可用与容量扩展的后续计划见 [GATEWAY_HA_PLAN.md](GATEWAY_HA_PLAN.md)。

---

## 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 语言 | Go 1.25 | 全后端开发 |
| RPC 框架 | gRPC (`google.golang.org/grpc`) | 网关 ↔ 客户端、网关/协调器 ↔ Node 通信 |
| 序列化 | Protocol Buffers (proto3) | gRPC 消息定义与编解码 |
| 冷数据存储 | PostgreSQL + GORM ORM | 用户账号、密码哈希、持久化 Profile |
| 热数据 / 控制面 | Redis (`go-redis`) | Topology、节点注册、会话、Boss、Checkpoint、选主与事件发布订阅 |
| 终端客户端 | ANSI Escape Codes + 原始终端模式 | 终端 TUI 渲染 |
| 浏览器展示 | HTML5 Canvas | 静态地图可视化页面；当前未接入 Gateway WebSocket |
| JSON 库 | `goccy/go-json` | Redis 数据交换 |

---

## 项目结构

```text
warzone/
├── frontend/
│   └── index.html              # 静态 Canvas 地图可视化页面
├── src/
│   ├── cmd/
│   │   ├── server/main.go      # Gateway：玩家 gRPC 流、认证、路由与状态推送
│   │   ├── coordinator/main.go # Coordinator：选主、拓扑、节点健康与故障转移
│   │   ├── node/main.go        # 游戏逻辑节点 (Node)
│   │   ├── client/main.go      # 终端客户端（ANSI TUI）
│   │   ├── admin/main.go       # 管理 CLI（查看 Gateway 状态）
│   │   └── benchmark/main.go   # gRPC 压测工具
│   ├── cluster/
│   │   ├── cluster.go          # Gateway 数据面：会话路由、地图缓存与事件
│   │   └── grpc_client.go      # Gateway/Coordinator 共用的 Node gRPC 客户端
│   ├── coordinator/            # 控制面：发现、心跳、拓扑与故障转移
│   ├── elector/                # Coordinator Redis leader lease
│   ├── node/                   # Node 主权校验、世界 tick、checkpoint、Node RPC
│   ├── world/                  # 游戏世界引擎：地图、玩家、NPC、宝物、战斗逻辑
│   ├── storage/                # PostgreSQL + Redis 存储、Topology 与 fencing
│   ├── protocol/               # 领域状态、常量与对象池
│   ├── pb/                     # Protobuf 消息与 Gateway/Admin/Node 服务定义
│   └── go.mod / go.sum
├── plan/                       # 已完成分层整改的设计与实施记录
├── start.sh                    # 本地 6 Node + Coordinator + Gateway 启动脚本
├── stop.sh                     # 本地进程停止与端口回收脚本
└── GATEWAY_HA_PLAN.md          # Gateway 高可用与容量扩展计划
```

---

## 系统架构

```text
┌─────────────────────────────────────────────────────────────┐
│                         客户端层                            │
│   Terminal TUI (cmd/client)      Admin CLI (cmd/admin)      │
└───────────────┬──────────────────────────┬──────────────────┘
                │ gRPC 双向流              │ gRPC
┌───────────────▼──────────────────────────▼──────────────────┐
│ Gateway（cmd/server + cluster/）                            │
│ - GameStream、认证、会话路由、状态推送                      │
│ - Redis Topology 缓存、Node 数据面连接                      │
│ - 地图快照缓存与 Redis Pub/Sub 事件订阅                     │
└───────────────┬───────────────────────────────┬─────────────┘
                │ 数据面 RPC                    │ Redis
┌───────────────▼──────────────────┐            │
│ Node（cmd/node）                 │            │
│ - 当前 owner：tick、状态写入、    │            │
│   checkpoint、MapEpoch fencing    │            │
└──────────────────────────────────┘            │
                                                 │
┌────────────────────────────────────────────────▼───────────┐
│ Coordinator（cmd/coordinator）                              │
│ - Redis 选主、节点发现、健康检查                            │
│ - Topology 提交、故障转移、Boss 生命周期                    │
└─────────────────────────────────────────────────────────────┘

PostgreSQL：用户与 Profile
Redis：Topology、会话、Checkpoint、选主、事件、节点注册
```

### 核心模块说明

#### 1. 网关（Gateway — `cmd/server/main.go`、`cluster/`）

- 对终端客户端提供 **gRPC `GameStream` 双向流**；首帧完成登录、注册或快速进入。
- 读取 Redis 中已提交的 Topology，建立到当前 owner Node 的数据面连接，完成认证、会话路由和玩家命令转发。
- 默认每 100ms 从本地地图缓存装配状态；地图缓存每 150ms 从各地图 owner 刷新一次。
- 对认证、命令确认和错误进行可靠排队；状态帧仅保留最新快照，避免慢客户端无限积压。
- 生命周期端点支持 `/healthz`、`/readyz` 与 `/drain`。

#### 2. 协调器（Coordinator — `cmd/coordinator`、`coordinator/`、`elector/`）

- 通过 Redis 租约选出唯一 leader；仅 leader 执行节点发现、Ping 健康检查、Topology 初始化与故障转移。
- 将版本化 Topology、MapEpoch 与会话迁移以 CAS 方式提交到 Redis，并发布拓扑变更通知。
- 负责全局 Boss 生命周期；不承接玩家高频命令，也不驱动节点世界 tick。

#### 3. 节点（Node — `node/`、`cmd/node/main.go`）

- 启动参数 `-id`、`-addr`、`-map` 只声明节点身份、地址和地图候选能力；主权以 Redis Topology 为准。
- 当前 owner 按运行时配置的间隔驱动 NPC/物品等世界模拟，发布地图事件并保存带 MapEpoch 的 checkpoint。
- 所有地图写入都校验 owner/epoch fence；失去主权、Topology 不可用或 drain 时 fail-closed。
- 每 3 秒通过 Redis 注册续约，约每 10 秒下沉活跃玩家热数据。

#### 4. 游戏世界（World — `world/world.go`）

- 基于字符网格的地图系统，内置 `green`、`cave`、`ruins` 三张地图（56×24）。
- 支持移动、攻击（NPC/PvP）、治疗、商店购买、世界 Boss、跨地图切换、死亡与复活。
- NPC 会自动寻找附近玩家攻击并随机移动；地图对象通过锁保护并发访问。

#### 5. 存储层（Store — `storage/`）

- **PostgreSQL `users` 表**：账号、bcrypt 密码哈希、持久化角色属性。
- **Redis**：
  - `battle:topology`：控制面提交的版本化地图 owner / epoch；
  - `global_sessions`：当前在线会话记录；
  - `hot_sessions`：位置、血量等热数据；
  - `checkpoints`：地图 checkpoint；
  - `boss:global:*`：世界 Boss 状态；
  - `events:*`：Pub/Sub 事件通道；
  - `battle:node:registry:*`：带 TTL 的节点注册表。

---

## 启动方式

```bash
# 推荐：启动本地六节点集群。
# 启动前确保 PostgreSQL、Redis 可访问；连接参数可通过 BW_* / BATTLEWORLD_* 环境变量覆盖。
./start.sh

# 手工启动时：先启动每张地图的两个候选 Node，再启动 Coordinator，最后启动 Gateway。
cd src
go run ./cmd/node -id node-a -addr 127.0.0.1:9311 -map green -lifecycle-addr 127.0.0.1:9411
go run ./cmd/node -id node-b -addr 127.0.0.1:9312 -map green -lifecycle-addr 127.0.0.1:9412
go run ./cmd/coordinator -lifecycle-addr 127.0.0.1:9421
go run ./cmd/server -lifecycle-addr 127.0.0.1:9422

# 终端客户端与压测
go run ./cmd/client
go run ./cmd/benchmark -u 800 -t 60

# 停止本地集群
cd .. && ./stop.sh
```

默认本地脚本为每张地图启动两个候选 Node：

| 地图 | 节点 |
|---|---|
| `green` | `node-a`、`node-b` |
| `cave` | `node-c`、`node-d` |
| `ruins` | `node-e`、`node-f` |

---

## 数据流示例

```text
用户按下 W 键（向上移动）：
  客户端 → gRPC ClientEnvelope{Move{DIRECTION_UP}} →
  Gateway → Cluster.Move() →
  按 Redis Topology 与 Session 确定当前 owner Node / MapEpoch →
  NodeGRPCClient.MovePlayer() →
  远端 Node 校验 owner/epoch fence → World.MovePlayer() →
  Gateway 返回 CommandResult →
  下一个 100ms 状态周期从本地地图缓存推送 WorldState 给客户端
```

## 已知演进方向

Gateway 当前仍以单实例本地脚本运行，并以完整 `WorldState` 进行常态推送。后续将通过多副本接入、Session Lease、连接治理、Gateway 侧地图增量同步和故障演练来避免接入单点、降低热点地图的推送成本。详细阶段、协议边界与验收条件见 [GATEWAY_HA_PLAN.md](GATEWAY_HA_PLAN.md)。
