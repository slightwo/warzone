# Battleworld — 分布式多人实时战斗游戏

## 项目概述

**Battleworld**（烬原战境）是一款基于 **Go + gRPC + Redis + PostgreSQL** 构建的分布式多人实时战斗游戏。玩家通过终端或浏览器接入，在多张地图中移动、战斗、拾取宝物、挑战世界首领，所有状态在分布式节点集群中实时同步。

---

## 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 语言 | Go 1.24 | 全后端开发 |
| RPC 框架 | gRPC (google.golang.org/grpc) | 节点间通信、网关 ↔ 客户端通信 |
| 序列化 | Protocol Buffers (proto3) | 消息定义与编解码 |
| 冷数据存储 | PostgreSQL + GORM ORM | 用户账号、密码、持久化 Profile |
| 热数据 / 缓存 | Redis (go-redis) | Session、Boss 状态、Checkpoint、节点注册、事件发布订阅 |
| 终端客户端 | ANSI Escape Codes + 原始终端模式 | 终端 TUI 渲染 |
| 浏览器客户端 | WebSocket + HTML5 Canvas | 网页端地图可视化 |
| JSON 库 | goccy/go-json | 高性能 JSON 序列化（Redis 数据交换） |

---

## 项目结构

```
warzone/
├── frontend/
│   └── index.html              # 浏览器 WebSocket 客户端（Canvas 渲染）
├── src/
│   ├── cmd/
│   │   ├── server/main.go      # 网关服务器 (Gateway)
│   │   ├── node/main.go        # 游戏逻辑节点 (Node)
│   │   ├── client/main.go      # 终端客户端（ANSI TUI）
│   │   ├── admin/main.go       # 管理 CLI（查看状态 / 模拟故障）
│   │   └── benchmark/main.go   # 压测工具
│   ├── cluster/
│   │   ├── cluster.go          # 集群协调器：节点发现、路由、故障转移、缓存、事件
│   │   └── grpc_client.go      # gRPC 远程节点客户端（NodeClient 接口实现）
│   ├── node/
│   │   ├── node.go             # 节点核心服务：地图实例管理、战斗处理
│   │   └── grpc_server.go      # 节点 gRPC 服务端（暴露 NodeService RPC）
│   ├── world/
│   │   └── world.go            # 游戏世界引擎：地图、玩家、NPC、宝物、战斗逻辑
│   ├── storage/
│   │   └── store.go            # 存储层：PostgreSQL + Redis 抽象
│   ├── protocol/
│   │   ├── message.go          # 消息类型定义、常量、连接封装、对象池
│   │   ├── world_state.go      # MapEvents 定义
│   │   └── adapter.go          # Protobuf ↔ Go struct 转换函数
│   ├── pb/
│   │   ├── battle.proto        # Protobuf 消息与服务定义
│   │   ├── battle.pb.go        # 生成的 Protobuf 消息代码
│   │   └── battle_grpc.pb.go   # 生成的 gRPC 服务代码
│   ├── go.mod / go.sum
│   └── server.exe / client.exe # 编译产物
└── tostart.txt                 # 启动命令参考
```

---

## 系统架构

### 分层架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                               │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│   │ Terminal TUI  │  │  Web Browser │  │  Admin CLI   │     │
│   │ (client/main) │  │ (index.html) │  │ (admin/main) │     │
│   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│          │ gRPC            │ WebSocket        │ TCP/JSON   │
├──────────┼─────────────────┼──────────────────┼────────────┤
│          ▼                 ▼                  ▼            │
│   ┌──────────────────────────────────────────────────┐     │
│   │             网关层 (server/main.go)               │     │
│   │   - gRPC GameStream (双向流)                       │     │
│   │   - WebSocket /ws 端点                             │     │
│   │   - 认证、Session 管理                              │     │
│   │   - 100ms 状态推送                                  │     │
│   └──────────────────────┬───────────────────────────┘     │
│                          │                                  │
│   ┌──────────────────────▼───────────────────────────┐     │
│   │            Cluster 协调层 (cluster/)               │     │
│   │  - 节点发现 (discoveryLoop)                        │     │
│   │  - 心跳检测 (heartbeatLoop)                        │     │
│   │  - 主/副本路由 & 故障转移                           │     │
│   │  - 缓存快照 (mapCacheLoop)                         │     │
│   │  - 后台模拟 (backgroundLoop)                       │     │
│   │  - 检查点 & 副本复制 (checkpointLoop)              │     │
│   └──┬────────────┬────────────┬──────────────────────┘     │
│      │            │            │                             │
│   ┌──▼──┐    ┌────▼────┐  ┌───▼───┐                        │
│   │NodeA│    │ NodeB   │  │ NodeC │    ← 物理节点层         │
│   │green│    │cave,ruin│  │(repl) │     (node/)             │
│   └──┬──┘    └────┬────┘  └───┬───┘                        │
│      │            │            │                             │
│   ┌──┴────────────┴────────────┴──┐                        │
│   │     数据存储层 (storage/)      │                        │
│   │  PostgreSQL ← 用户冷数据       │                        │
│   │  Redis ← Session/Checkpoint/  │                        │
│   │          Boss/事件总线/节点注册 │                        │
│   └───────────────────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### 核心模块说明

#### 1. 网关 (Gateway — `cmd/server/main.go`)
- 提供两个接入点：**gRPC GameStream 双向流**（终端客户端使用）和 **WebSocket `/ws`**（浏览器使用）
- 认证流程：接收登录/注册/快速进入 → 调用 Cluster 认证 → 开始 100ms 定时状态推送
- 读/写分离：接收协程处理客户端指令，发送协程和心跳协程异步推送状态

#### 2. 集群协调器 (Cluster — `cluster/cluster.go`)
- **节点发现** (`discoveryLoop`, 1s)：通过 Redis 扫描 `battle:node:*` 前缀，动态发现新节点并建立 gRPC 连接
- **心跳检测** (`heartbeatLoop`, 1s)：对所有已知节点发起 Ping，发现故障则触发 `handleNodeFailure`
- **地图缓存** (`mapCacheLoop`, 150ms)：并行调用所有节点获取快照，缓存到本地
- **后台模拟** (`backgroundLoop`, 700ms)：驱动所有节点上的 NPC 移动、攻击、物品刷新
- **检查点** (`checkpointLoop`, 700ms)：定期将主节点快照写入 Redis 并复制到副本节点
- **故障转移** (`handleNodeFailure`)：将失效节点的主地图提升到原副本节点，重新分配副本
- **事件系统**：通过 Redis Pub/Sub 实现全局/地图/用户三级事件广播

#### 3. 节点 (Node — `node/node.go`, `cmd/node/main.go`)
- 通过命令行参数指定 `--id`, `--addr`, `--maps`(主地图), `--replicas`(副本地图)
- 本地持有 `map[string]*world.World` 管理多地图实例
- 每 3 秒通过 Redis 上报心跳（含节点 ID、地址、主/副地图列表）
- 每 10 秒将活跃玩家热数据异步刷新到 Redis

#### 4. 游戏世界 (World — `world/world.go`)
- 基于字符网格的地图系统，3 张预设地图（56×24 大小）
- 核心玩法：移动、攻击（NPC/PvP）、治疗、商店（购买药剂/武器）、世界 Boss 挑战、跨地图切换
- NPC 行为：自动寻找最近玩家攻击、随机徘徊
- 玩家状态：复活倒计时、战场播报

#### 5. 存储层 (Store — `storage/store.go`)
- **PostgreSQL (users 表)**：用户账号、密码哈希、持久化角色属性
- **Redis**：
  - `global_sessions`: 在线用户会话（用户名 → 地图/节点映射）
  - `hot_sessions`: 热数据（位置、血量等）
  - `checkpoints`: 地图检查点快照
  - `boss:global:hp` / `boss:global:state`: 世界 Boss 状态
  - `events:*`: Pub/Sub 事件通道
  - `battle:node:registry:*`: 节点注册表（含 TTL）

---

## 启动方式

```bash
# 1. 启动网关 (Gateway)
cd src && go run ./cmd/server

# 2. 启动节点 (Node) — 三个节点示例
go run ./cmd/node -id "node-a" -maps "green" -replicas "cave" -addr "127.0.0.1:9311"
go run ./cmd/node -id "node-b" -maps "cave,ruins" -addr "127.0.0.1:9312"
go run ./cmd/node -id "node-c" -replicas "green,ruins" -addr "127.0.0.1:9313"

# 3. 启动客户端
go run ./cmd/client                # 终端 TUI 客户端
# 或浏览器访问 http://localhost:9091 （网关 WebSocket 端口）

# 4. 管理工具
go run ./cmd/admin 状态             # 查看集群状态
go run ./cmd/admin 故障 node-a      # 模拟节点故障
go run ./cmd/admin 恢复 node-a      # 恢复节点

# 5. 压测
go run ./cmd/benchmark -u 800 -t 60
```

---

## 数据流示例

```
用户按下 W 键（向上移动）：
  客户端 → gRPC Message{Type:"move", Dir:"up"} → 
  网关 → Cluster.Move() → 
  查找 Session 获取对应 Node → 
  NodeGRPCClient.MovePlayer() → 
  远端 Node 的 MovePlayer → World.MovePlayer() → 
  返回 Profile → 
  下一个 100ms 心跳推送完整 WorldState 给客户端
```
